<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MyApplication">

        <!-- Google Maps Configuration -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Branch.io SDK Configuration -->
        <meta-data
            android:name="io.branch.sdk.BranchKey"
            android:value="@string/branch_key" />
        <meta-data
            android:name="io.branch.sdk.BranchKey.test"
            android:value="@string/branch_key_test" />
        <meta-data
            android:name="io.branch.sdk.TestMode"
            android:value="false" />

        <!-- Digital Asset Links for Passkey Support -->
        <meta-data
            android:name="asset_statements"
            android:value="@string/asset_statements" />

        <activity
            android:name=".ui.dashboard.HomeActivity"
            android:exported="false"
            android:launchMode="singleTask" />
            <!-- Note: HomeActivity no longer handles deep links directly.
                 All deep links are routed through LauncherScreenActivity which
                 acts as a single gate and navigates to HomeActivity with the event. -->
        <activity
            android:name=".ui.legal.LegalDocumentActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.LauncherScreenActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Branch.io Deep Link Intent Filters -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="foodchoo.app.link"
                    android:scheme="https" />
                <data
                    android:host="foodchoo-alternate.app.link"
                    android:scheme="https" />
            </intent-filter>
        </activity>
    </application>

</manifest>